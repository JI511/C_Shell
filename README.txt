Run make or make shell to compile
use ./shell to execute

Notes:
When using !! the command executed is added to history. For example if !! is executed and ls was the last command then ls is added to history.
History is limited to 100.
History is added to history when run.